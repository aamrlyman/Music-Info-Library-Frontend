import SearchBar from "../SearchBar/SearchBar";

const NavBar = (props) => {
  return (
    <div className ="nav">
      <nav>
        <h1>Music Library</h1>
      </nav>
    </div>
  );
};

export default NavBar;
